apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: freshlab-http-redirect
  namespace: istio-system
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: freshlab
      sectionName: http
  hostnames:
    - "*.kleinsorge.dev"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
---
{{- range .Values.appRoutes }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .namespace }}
  labels:
    istio.io/dataplane-mode: {{ .dataplaneMode | default "ambient" | quote }}
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/enforce-version: latest
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/audit-version: latest
    pod-security.kubernetes.io/warn: restricted
    pod-security.kubernetes.io/warn-version: latest
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .name }}
  namespace: {{ .namespace }}
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: freshlab
      namespace: istio-system
      sectionName: https
  hostnames:
    - {{ .hostname | quote }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: {{ .service }}
          port: {{ .port }}
          weight: 1
---
{{- end }}
