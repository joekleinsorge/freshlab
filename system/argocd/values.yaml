argo-cd:
  global:
    domain: argocd.kleinsorge.dev
  redisSecretInit:
    # The Redis secret is already managed by the bootstrap and the completed
    # Helm hook only creates permanent Argo drift after every upgrade.
    enabled: false
  configs:
    params:
      server.insecure: true
      controller.diff.server.side: false
      applicationsetcontroller.enable.progressive.syncs: true
    cm:
      kustomize.buildOptions: --enable-alpha-plugins --enable-exec
      oidc.config: |
        name: Dex
        issuer: https://dex.kleinsorge.dev
        clientID: argocd-sso
        clientSecret: $argocd-oidc:clientSecret
        requestedScopes: ["openid", "profile", "email", "groups"]
      resource.ignoreResourceUpdatesEnabled: true
      resource.customizations.ignoreResourceUpdates.all: |
        jsonPointers:
          - /status
      # istiod deliberately fills in CA data and tightens its validators after
      # Helm creates them. Treat those runtime-owned webhook bodies as live
      # controller state instead of permanent Git drift.
      resource.customizations.ignoreDifferences.admissionregistration.k8s.io_ValidatingWebhookConfiguration: |
        jqPathExpressions:
          - '.webhooks[]? | select(.name == "rev.validation.istio.io")'
          - '.webhooks[]? | select(.name == "validation.istio.io")'
    rbac:
      policy.default: role:readonly
      policy.csv: |
        g, freshlab-admins, role:admin
      scopes: '[groups, email]'
  server:
    replicas: 2
    autoscaling:
      enabled: true
      minReplicas: 2
      maxReplicas: 5
      targetCPUUtilizationPercentage: 80
    ingress:
      enabled: false
      ingressClassName: istio
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        hajimari.io/appName: Argo CD
        hajimari.io/icon: kubernetes
      tls: true
    resources:
      requests:
        cpu: 250m
        memory: 512Mi
      limits:
        cpu: "1"
        memory: 1Gi
    metrics: &metrics
      enabled: true
      serviceMonitor:
        enabled: true
  dex:
    enabled: false
  controller:
    replicas: 2
    resources:
      requests:
        cpu: 250m
        memory: 512Mi
      limits:
        cpu: "1"
        memory: 1Gi
    metrics: *metrics
  repoServer:
    replicas: 2
    env:
      - name: SOPS_AGE_KEY_FILE
        value: /home/argocd/.config/sops/age/keys.txt
      # KSOPS invokes its executable by name, so make the shared plugin
      # directory explicit in the repo-server's executable search path.
      - name: PATH
        value: /usr/local/bin:/usr/local/sbin:/usr/sbin:/usr/bin:/sbin:/bin
    initContainers:
      - name: install-ksops
        image: viaductoss/ksops:v4.5.1@sha256:4def9fdd4e2f850265740ebe9592c5455d19b76891e88e602df8b52d74b95334
        command:
          - /usr/local/bin/ksops
          - install
          - --with-kustomize
          - /custom-tools
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          runAsNonRoot: true
          runAsUser: 999
          seccompProfile:
            type: RuntimeDefault
        volumeMounts:
          - name: custom-tools
            mountPath: /custom-tools
    volumeMounts:
      - name: custom-tools
        mountPath: /usr/local/bin/kustomize
        subPath: kustomize
      - name: custom-tools
        mountPath: /usr/local/bin/ksops
        subPath: ksops
      - name: sops-age
        mountPath: /home/argocd/.config/sops/age
        readOnly: true
    volumes:
      - name: custom-tools
        emptyDir: {}
      - name: sops-age
        secret:
          secretName: sops-age
    resources:
      requests:
        cpu: 250m
        memory: 512Mi
      limits:
        cpu: "1"
        memory: 1Gi
    metrics: *metrics
  redis:
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 512Mi
    metrics: *metrics
  applicationSet:
    replicas: 2
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 512Mi
    metrics: *metrics
  notifications:
    enabled: true
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 512Mi
    metrics: *metrics
argocd-apps:
  projects:
    system:
      namespace: argocd
      description: Trusted cluster controllers and cluster-scoped infrastructure
      sourceRepos:
        - https://github.com/joekleinsorge/freshlab
      destinations:
        - namespace: '*'
          server: https://kubernetes.default.svc
      clusterResourceWhitelist:
        - group: '*'
          kind: '*'
    platform:
      namespace: argocd
      description: Shared identity, observability, and automation services
      sourceRepos:
        - https://github.com/joekleinsorge/freshlab
      destinations:
        - namespace: arc
          server: https://kubernetes.default.svc
        - namespace: authelia
          server: https://kubernetes.default.svc
        - namespace: dex
          server: https://kubernetes.default.svc
        - namespace: grafana
          server: https://kubernetes.default.svc
        - namespace: runners
          server: https://kubernetes.default.svc
        - namespace: tailscale
          server: https://kubernetes.default.svc
      clusterResourceWhitelist:
        - group: rbac.authorization.k8s.io
          kind: ClusterRole
        - group: rbac.authorization.k8s.io
          kind: ClusterRoleBinding
    apps:
      namespace: argocd
      description: User-facing homelab workloads
      sourceRepos:
        - https://github.com/joekleinsorge/freshlab
      destinations:
        - namespace: '*'
          server: https://kubernetes.default.svc
      clusterResourceWhitelist:
        - group: ''
          kind: PersistentVolume
        - group: rbac.authorization.k8s.io
          kind: ClusterRole
        - group: rbac.authorization.k8s.io
          kind: ClusterRoleBinding
        - group: storage.k8s.io
          kind: StorageClass
    freshlab-secrets:
      namespace: argocd
      description: SOPS-encrypted Secrets reconciled by KSOPS
      sourceRepos:
        - https://github.com/joekleinsorge/freshlab
      destinations:
        - namespace: '*'
          server: https://kubernetes.default.svc
      clusterResourceWhitelist:
        - group: ''
          kind: Namespace
      namespaceResourceWhitelist:
        - group: ''
          kind: Secret
  applicationsets:
    root:
      namespace: argocd
      strategy:
        type: RollingSync
        rollingSync:
          steps:
            - matchExpressions:
                - key: freshlab.kleinsorge.dev/application
                  operator: In
                  values:
                    - argocd
              maxUpdate: 100%
            - matchExpressions:
                - key: freshlab.kleinsorge.dev/layer
                  operator: In
                  values:
                    - freshlab-secrets
              maxUpdate: 100%
            - matchExpressions:
                - key: freshlab.kleinsorge.dev/layer
                  operator: In
                  values:
                    - system
                - key: freshlab.kleinsorge.dev/application
                  operator: NotIn
                  values:
                    - argocd
              maxUpdate: 100%
            - matchExpressions:
                - key: freshlab.kleinsorge.dev/layer
                  operator: In
                  values:
                    - platform
              maxUpdate: 100%
            - matchExpressions:
                - key: freshlab.kleinsorge.dev/layer
                  operator: In
                  values:
                    - apps
              maxUpdate: 100%
      goTemplate: true
      goTemplateOptions:
        - missingkey=error
      generators:
        - git:
            repoURL: &repoURL https://github.com/joekleinsorge/freshlab
            revision: &revision main
            directories:
              - path: system/*
              - path: system/gateway-api-crds
                exclude: true
              - path: platform/*
              - path: global/*
              - path: apps/*
              - path: freshlab-secrets
      template:
        metadata:
          name: '{{.path.basename}}'
          labels:
            freshlab.kleinsorge.dev/application: '{{.path.basename}}'
            freshlab.kleinsorge.dev/layer: '{{index .path.segments 0}}'
          annotations:
            link.argocd.argoproj.io/external-link: '{{- $links := dict "argocd" "Open app|https://argocd.kleinsorge.dev" "argo-rollouts" "Open app|https://rollouts.kleinsorge.dev" "authelia" "Open app|https://authelia.kleinsorge.dev" "budget" "Open app|https://budget.kleinsorge.dev" "dex" "Open app|https://dex.kleinsorge.dev" "disney-alerts" "Open app|https://disney-alerts.kleinsorge.dev" "excalidraw" "Open app|https://draw.kleinsorge.dev" "flask" "Open app|https://flask.kleinsorge.dev" "freshrss" "Open app|https://rss.kleinsorge.dev" "frigate" "Open app|https://frigate.kleinsorge.dev" "grafana" "Open app|https://grafana.kleinsorge.dev" "grocy" "Open app|https://grocy.kleinsorge.dev" "hajimari" "Open app|https://home.kleinsorge.dev" "immich" "Open app|https://photos.kleinsorge.dev" "kindle-weather" "Open app|https://kindle-weather.kleinsorge.dev" "kitchenowl" "Open app|https://kitchenowl.kleinsorge.dev" "kiali" "Open app|https://kiali.kleinsorge.dev" "kube-ops-view" "Open app|https://view.kleinsorge.dev" "librespeed" "Open app|https://speed.kleinsorge.dev" "mealie" "Open app|https://mealie.kleinsorge.dev" "nginx" "Open app|https://web.kleinsorge.dev" "plex" "Open app|https://plex.kleinsorge.dev" "seafile" "Open app|https://files.kleinsorge.dev" "tools" "Open app|https://tools.kleinsorge.dev" "uptime-kuma" "Open app|https://status.kleinsorge.dev" -}}{{get $links .path.basename}}'
        spec:
          destination:
            name: in-cluster
            namespace: '{{.path.basename}}'
          project: '{{index .path.segments 0}}'
          # Reloader intentionally injects content-hash environment variables
          # to trigger rolling restarts. They are controller-owned runtime
          # state, not configuration drift from Git.
          ignoreDifferences:
            - group: apps
              kind: Deployment
              jqPathExpressions:
                - '.spec.template.spec.containers[]?.env[]? | select(.name | startswith("STAKATER_"))'
            - group: apps
              kind: StatefulSet
              jqPathExpressions:
                - '.spec.template.spec.containers[]?.env[]? | select(.name | startswith("STAKATER_"))'
            # These CRD metadata fields are rewritten by the bundled
            # operators during installation and upgrades; the schemas remain
            # managed by the chart.
            - group: apiextensions.k8s.io
              kind: CustomResourceDefinition
              jqPathExpressions:
                - '.metadata.labels'
                - '.metadata.annotations'
            # cert-manager rotates this webhook bundle and the generated TLS
            # Secret independently of Helm.
            - group: admissionregistration.k8s.io
              kind: ValidatingWebhookConfiguration
              name: longhorn-snapshot-validation-webhook
              jqPathExpressions:
                - '.webhooks[]?.clientConfig.caBundle'
            - group: ''
              kind: Secret
              name: longhorn-snapshot-validation-webhook-tls
              jqPathExpressions:
                - '.data'
            # Grafana's external secret controller owns the password data;
            # ignore only its derived checksum and generated Secret payload.
            - group: ''
              kind: Secret
              name: grafana
              jqPathExpressions:
                - '.data'
            - group: apps
              kind: Deployment
              name: grafana
              jqPathExpressions:
                - '.spec.template.metadata.annotations["checksum/secret"]'
          source:
            repoURL: *repoURL
            path: '{{.path.path}}'
            targetRevision: *revision
          syncPolicy:
            automated:
              enabled: true
              prune: true
              selfHeal: true
            retry:
              limit: 10
              backoff:
                duration: 1m
                factor: 2
                maxDuration: 16m
            syncOptions:
              - CreateNamespace=true
              - ApplyOutOfSyncOnly=true
              - ServerSideApply=true
              - RespectIgnoreDifferences=true
      syncPolicy:
        preserveResourcesOnDeletion: true
argo-workflows:
  server:
    authModes:
      - sso
    sso:
      enabled: true
      issuer: https://dex.kleinsorge.dev
      clientId:
        name: argo-server-sso
        key: client-id
      clientSecret:
        name: argo-server-sso
        key: client-secret
      redirectUrl: https://workflows.kleinsorge.dev/oauth2/callback
      rbac:
        enabled: true
        secretWhitelist:
          - argo-workflows-sso-admin.service-account-token
          - argo-workflows-sso-readonly.service-account-token
      scopes:
        - openid
        - profile
        - email
        - groups
    ingress:
      enabled: false
      className: istio
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        hajimari.io/appName: Argo Workflows
        hajimari.io/icon: source-branch
      hosts:
        - workflows.kleinsorge.dev
      tls:
        - hosts:
            - workflows.kleinsorge.dev
          secretName: argo-workflows-tls-certificate

argocdGateway:
  enabled: true
  host: argocd.kleinsorge.dev
  address: 192.168.1.230
  serviceName: argocd-server
  servicePort: 80
  tlsSecretName: argocd-gateway-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  loadBalancerPool: ""

argoWorkflowsGateway:
  enabled: true
  host: workflows.kleinsorge.dev
  address: 192.168.1.229
  serviceName: argocd-argo-workflows-server
  servicePort: 2746
  tlsSecretName: argo-workflows-gateway-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  loadBalancerPool: ""
# argo-rollouts:
#   server:
#     ingress:
#       enabled: true
#       className: istio
#       annotations:
#         cert-manager.io/cluster-issuer: letsencrypt-prod
#       hosts:
#         - rollouts.kleinsorge.dev
#       tls:
#         - hosts:
#             - rollouts.kleinsorge.dev
#           secretName: argo-rollouts-tls-certificate
