argo-rollouts:
  installCRDs: true
  clusterInstall: true
  controller:
    # Keep the existing component name while upgrading the controller.
    component: server
    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        memory: 512Mi
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
  podSecurityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  containerSecurityContext:
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop:
        - ALL
  dashboard:
    enabled: true
    # The v1.10 dashboard spins indefinitely when its default namespace has
    # no Rollouts. Start on weather; Flask remains available in the selector.
    extraArgs:
      - --namespace
      - kindle-weather
    httproute:
      enabled: true
      annotations:
        link.argocd.argoproj.io/external-link: "Open app|https://rollouts.kleinsorge.dev"
      parentRefs:
        - group: gateway.networking.k8s.io
          kind: Gateway
          name: freshlab
          namespace: istio-system
          sectionName: https
      hostnames:
        - rollouts.kleinsorge.dev
