node-feature-discovery:
  master:
    resources:
      requests:
        cpu: 25m
        memory: 64Mi
      limits:
        cpu: 200m
        memory: 256Mi
  worker:
    # An empty JSON object preserves the chart's valid default worker config.
    # The chart serializes this field into the config file consumed by NFD.
    config: {}
    resources:
      requests:
        cpu: 25m
        memory: 64Mi
      limits:
        cpu: 200m
        memory: 256Mi
