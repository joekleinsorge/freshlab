apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: freshlab-node-agent
value: 100000
globalDefault: false
preemptionPolicy: PreemptLowerPriority
description: >-
  Ensures node-labeling agents remain available on each node without using a
  Kubernetes-reserved system priority or preempting storage controllers.
